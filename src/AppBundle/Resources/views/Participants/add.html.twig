{% extends "base.html.twig" %}

{% block title %}Merrick Quizz|Participants{% endblock %}

{% block body %}

	{{ form_start(form) }}
		{{ form_widget(form) }}
	{{ form_end(form) }}
	{#
	<div class="form-group">
		<form method="post">
    	<label for="addForm">Enter your username to save your score.</label><br>
    	<input type="text" id="addForm" class="form-control" name="add_participant" placeholder="Your username"><br>
    	<button type="submit" class="btn btn-primary">Save</button>
</form>
	</div>
#}
	{% for message in app.session.flashbag.get('success') %}
		{{ message }}
	{% endfor %}
{% endblock %}


